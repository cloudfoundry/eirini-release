---
apiVersion: v1
kind: Service
metadata:
  name: eirini-api
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: {{ .Values.api.tls_port }}
      protocol: TCP
      name: https
  selector:
    name: eirini-api
